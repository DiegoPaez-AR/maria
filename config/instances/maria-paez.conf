# Configuración de la instancia Maria Paez.
# Formato KEY=VALUE (sin espacios alrededor del =, valores entre comillas si tienen espacios).
# Cargado por ecosystem.config.js (al arrancar pm2) y por ops/cron-master.sh.

# ─── Identidad ───────────────────────────────────────────────────────────
ASISTENTE_NOMBRE="Maria Paez"
ASISTENTE_SLUG=maria-paez
ASISTENTE_FROM_EMAIL=maria.paez.secre@gmail.com
ASISTENTE_TZ=America/Argentina/Buenos_Aires

# ─── Owner ─────────────────────────────────────────────────────────────────
OWNER_NOMBRE=Diego
OWNER_EMAIL=diego@paez.is
OWNER_WA=541132317896@c.us
OWNER_CALENDAR_ID=diego@paez.is

# ─── Cap de usuarios (vacío = ilimitado) ─────────────────────────────────
# ASISTENTE_MAX_USUARIOS=10

# ─── Paths de estado (legacy: en raíz; se migran a state/maria-paez/ cuando aparezca 2da instancia) ───
MARIA_DB=/root/secretaria/state/maria-paez/db/maria.sqlite
GOOGLE_TOKEN_PATH=/root/secretaria/state/maria-paez/token.json
GOOGLE_CRED_PATH=/root/secretaria/state/maria-paez/credentials.json
WA_AUTH_DIR=/root/secretaria/state/maria-paez/.wwebjs_auth

# ─── Tunings (todos tienen defaults sanos en el código) ─────────────────
WA_DEBOUNCE_MS=10000
GMAIL_POLL_MS=300000
CALENDAR_WATCH_MS=28800000

# ─── Cuenta de Claude ────────────────────────────────────────────────────
# Heredada del VPS (la cuenta de Diego). No setear nada.
# Para usar API key directa: ANTHROPIC_API_KEY=sk-ant-...
# Para usar settings file con OAuth de otra cuenta: CLAUDE_SETTINGS_FILE=...
