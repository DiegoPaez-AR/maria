# ejecutado: 2026-08-03T16:32:22-03:00
# host: ubuntu-4gb-nbg1-2
# instancia: maria-paez
# script:
#   #!/bin/bash
#   cd /root/secretaria
#   pm2 reload ecosystem.config.js --only maria-paez --update-env >/dev/null 2>&1
#   sleep 5
#   set -a; . config/instances/maria-paez.conf 2>/dev/null; . config/secrets.conf 2>/dev/null; set +a
#   node - <<'NODE'
#   (async () => {
#     const mem = require('/root/secretaria/memory');
#     const gc = require('/root/secretaria/google-contacts');
#     const dormir = (ms) => new Promise(r => setTimeout(r, ms));
#     const rows = mem.db.prepare(`SELECT c.*, u.nombre AS dueno_nombre FROM contactos c JOIN usuarios u ON u.id = c.usuario_id WHERE c.cumple IS NOT NULL AND c.cumple != '' ORDER BY c.id`).all();
#     let ok = 0, fail = 0;
#     for (const c of rows) {
#       try { await gc.sincronizarContacto(c, { dueno: c.dueno_nombre }); ok++; } catch (e) { fail++; console.log('✗', c.nombre, ':', e.message.slice(0, 70)); }
#       await dormir(700);
#     }
#     console.log(`re-sync cumples: ${ok} ok, ${fail} fallidos de ${rows.length} contactos con cumple`);
#   })().catch(e => { console.error('FALLO:', e.message); process.exit(1); });
#   NODE

# ───── output ─────
re-sync cumples: 97 ok, 0 fallidos de 97 contactos con cumple

# exit=0
