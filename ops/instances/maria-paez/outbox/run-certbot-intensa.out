# ejecutado: 2026-05-17T19:13:01-03:00
# host: ubuntu-4gb-nbg1-2
# instancia: maria-paez
# script:
#   #!/bin/bash
#   # Emite cert Let's Encrypt para intensa.io + www.intensa.io y configura
#   # redirect HTTP→HTTPS en el vhost nginx. Idempotente: si el cert ya existe,
#   # certbot lo detecta.
#   set +e
#   
#   echo "═══ Verificar DNS antes ═══"
#   for h in intensa.io www.intensa.io; do
#     IP=$(dig +short $h @1.1.1.1 | head -1)
#     if [ "$IP" != "178.104.166.91" ]; then
#       echo "✗ $h apunta a $IP (esperado 178.104.166.91) — abortando"
#       exit 1
#     fi
#     echo "✓ $h → $IP"
#   done
#   
#   echo ""
#   echo "═══ Correr certbot (modo --nginx, no-interactive) ═══"
#   certbot --nginx \
#     -d intensa.io \
#     -d www.intensa.io \
#     --redirect \
#     --non-interactive \
#     --agree-tos \
#     -m diego@paez.is \
#     --no-eff-email 2>&1
#   
#   echo ""
#   echo "═══ Estado certificados ═══"
#   certbot certificates 2>&1 | grep -E "Certificate Name|Domains|Expiry|Path" | head -20
#   
#   echo ""
#   echo "═══ nginx -t y reload (por las dudas) ═══"
#   nginx -t
#   systemctl reload nginx
#   systemctl is-active nginx
#   
#   echo ""
#   echo "═══ Smoke tests ═══"
#   curl -s -o /dev/null -w "intensa.io          HTTP(:80) %{http_code} (debería 301)\n" -H "Host: intensa.io" http://127.0.0.1/
#   curl -sk -o /dev/null -w "intensa.io          HTTPS    %{http_code} (debería 200)\n" -H "Host: intensa.io" https://127.0.0.1/
#   curl -sk -o /dev/null -w "www.intensa.io      HTTPS    %{http_code} (debería 200)\n" -H "Host: www.intensa.io" https://127.0.0.1/
#   curl -sk -o /dev/null -w "veritas-trace.com   HTTPS    %{http_code} (debería 200 — no se rompió)\n" -H "Host: www.veritas-trace.com" https://127.0.0.1/
#   
#   echo ""
#   echo "═══ DONE ═══"

# ───── output ─────
═══ Verificar DNS antes ═══
✓ intensa.io → 178.104.166.91
✗ www.intensa.io apunta a intensa.io. (esperado 178.104.166.91) — abortando

# exit=0
