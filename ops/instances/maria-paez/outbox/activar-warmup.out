# ejecutado: 2026-08-21T23:43:02-03:00
# host: ubuntu-4gb-nbg1-2
# instancia: maria-paez
# script:
#   #!/bin/bash
#   cd /root/secretaria
#   [ -f state/.canary-bad-commit ] && { echo "CANARY BLOQUEÓ: $(cat state/.canary-bad-commit)"; exit 0; }
#   CONF=config/instances/maria-paez.conf
#   # fuera el apagón total, entra el warm-up
#   sed -i '/^WA_SALIENTE_OFF=/d' "$CONF"
#   grep -q "^WA_WARMUP=" "$CONF" || { echo "" >> "$CONF"; echo "# 21/8: número NUEVO de Maria (+5491166446137) — warm-up: responde pero NO inicia chats" >> "$CONF"; echo "WA_WARMUP=1" >> "$CONF"; }
#   grep -E "^WA_(WARMUP|SALIENTE_OFF)=" "$CONF"
#   # el marker viejo de WA apagado (era de wwebjs) no molesta al bridge, pero lo dejo constar
#   ls state/maria-paez/wa-apagado 2>/dev/null && echo "(marker wa-apagado presente — solo afecta a wwebjs, no al bridge)"
#   pm2 reload ecosystem.config.js --only maria-paez --update-env >/dev/null 2>&1 && echo "reload OK — WARM-UP activo (responde sí, inicia no)"
#   if pgrep -f mb-build-worker >/dev/null; then echo "build corriendo"; else nohup bash /root/mb-build-worker.sh >/dev/null 2>&1 & echo "build v3.8 lanzado"; fi
#   echo LISTO

# ───── output ─────
WA_WARMUP=1
state/maria-paez/wa-apagado
(marker wa-apagado presente — solo afecta a wwebjs, no al bridge)
reload OK — WARM-UP activo (responde sí, inicia no)
build v3.8 lanzado
LISTO

# exit=0
