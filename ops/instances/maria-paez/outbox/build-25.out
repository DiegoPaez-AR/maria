# ejecutado: 2026-08-17T00:47:02-03:00
# host: ubuntu-4gb-nbg1-2
# instancia: maria-paez
# script:
#   #!/bin/bash
#   cd /root/secretaria
#   [ -f state/.canary-bad-commit ] && { echo "CANARY BLOQUEÓ: $(cat state/.canary-bad-commit)"; exit 0; }
#   pm2 reload ecosystem.config.js --only maria-paez --update-env >/dev/null 2>&1 && echo "reload server OK (/mbfallo activo)"
#   if pgrep -f mb-build-worker >/dev/null; then echo "build corriendo"; else nohup bash /root/mb-build-worker.sh >/dev/null 2>&1 & echo "build v2.5 lanzado"; fi

# ───── output ─────
reload server OK (/mbfallo activo)
build v2.5 lanzado

# exit=0
