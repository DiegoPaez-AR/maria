# ejecutado: 2026-05-10T21:28:02-03:00
# host: ubuntu-4gb-nbg1-2
# instancia: maria-paez
# script:
#   #!/bin/bash
#   set +e
#   cd /root/secretaria
#   set -a
#   source <(grep -E '^[A-Z_]+=' /root/secretaria/config/instances/maria-paez.conf)
#   set +a
#   
#   echo "=== a) pm2 estado ==="
#   pm2 jlist 2>/dev/null | python3 -c "
#   import json,sys
#   ps=json.load(sys.stdin)
#   for p in ps:
#       if p.get('name')!='maria-paez': continue
#       e=p.get('pm2_env',{})
#       print('status', e.get('status'), 'restarts', e.get('restart_time'))"
#   
#   echo
#   echo "=== b) test memory.existeEmailEntrante ==="
#   node << 'NODE_EOF'
#   const m = require('./memory');
#   // Buscamos un messageId real entre los emails entrantes recientes.
#   const real = m.db.prepare(`
#     SELECT metadata_json FROM eventos
#     WHERE canal='gmail' AND direccion='entrante' AND metadata_json IS NOT NULL
#     ORDER BY id DESC LIMIT 1
#   `).get();
#   let realId = null;
#   if (real) {
#     try {
#       const meta = JSON.parse(real.metadata_json);
#       realId = meta.messageId;
#     } catch {}
#   }
#   console.log('  realId encontrado:', realId);
#   console.log('  existeEmailEntrante(realId) →', realId ? m.existeEmailEntrante(realId) : 'no testeado');
#   console.log('  existeEmailEntrante("messageId-falso-12345") →', m.existeEmailEntrante('messageId-falso-12345'));
#   console.log('  existeEmailEntrante(null) →', m.existeEmailEntrante(null));
#   console.log('  existeEmailEntrante("") →', m.existeEmailEntrante(''));
#   console.log('  existeEmailEntrante("%con-%-y-_" /* like injection */) →', m.existeEmailEntrante('%con-%-y-_'));
#   NODE_EOF
#   
#   echo
#   echo "=== c) test executor.responderEmail con messageId falso ==="
#   node << 'NODE_EOF'
#   const exe = require('./executor');
#   const usuarios = require('./usuarios');
#   const owner = usuarios.obtenerOwner();
#   const ctx = { usuario: owner };
#   (async () => {
#     try {
#       await exe.ejecutarAcciones([{ tipo: 'responder_email', messageId: 'messageId-totalmente-inventado-xyz', texto: 'pwned' }], ctx);
#       console.log('  [FAIL] no rechazó messageId falso');
#     } catch (e) {
#       console.log('  [OK] rechazó:', e.message.slice(0, 200));
#     }
#   })();
#   NODE_EOF

# ───── output ─────
=== a) pm2 estado ===
status online restarts 12

=== b) test memory.existeEmailEntrante ===
  realId encontrado: 19e13b512e8231f0
  existeEmailEntrante(realId) → true
  existeEmailEntrante("messageId-falso-12345") → false
  existeEmailEntrante(null) → false
  existeEmailEntrante("") → false
  existeEmailEntrante("on-%-y-_" /* like injection */) →

=== c) test executor.responderEmail con messageId falso ===
[executor] acción #0 (responder_email) falló: responder_email: messageId "messageId-totalmente-inventado-xyz" no corresponde a ningún email recibido. Si querés mandar un email NUEVO, usá enviar_email.
  [FAIL] no rechazó messageId falso

# exit=0
