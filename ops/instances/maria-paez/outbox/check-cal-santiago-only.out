# ejecutado: 2026-05-16T19:57:02-03:00
# host: ubuntu-4gb-nbg1-2
# instancia: maria-paez
# script:
#   #!/bin/bash
#   # Chequeo puntual del calendar de Santiago + Diego + Hernán
#   set +e
#   set -a
#   . /root/secretaria/config/instances/maria-paez.conf
#   set +a
#   cd /root/secretaria
#   
#   echo "═══ Estado DB ═══"
#   sqlite3 -header -column "$MARIA_DB" "
#   SELECT id, nombre, calendar_id, calendar_acceso
#   FROM usuarios
#   WHERE nombre IN ('Santiago Bignone','Diego','Hernan Fulco')
#   ORDER BY id
#   "
#   
#   echo ""
#   echo "═══ Chequeo real contra Google (timeout 60s) ═══"
#   timeout 60s node -e "
#   (async () => {
#     const usuarios = require('./usuarios');
#     const g = require('./google');
#     for (const nombre of ['Diego', 'Hernan Fulco', 'Santiago Bignone']) {
#       const u = usuarios.listarActivos().find(x => x.nombre === nombre);
#       if (!u) { console.log(nombre, ': no encontrado'); continue; }
#       if (!u.calendar_id) { console.log(nombre, ': sin calendar_id'); continue; }
#       try {
#         const t0 = Date.now();
#         const r = await g.chequearAccesoCalendar(u.calendar_id);
#         const dt = Date.now() - t0;
#         console.log(\`\${nombre.padEnd(20)} calendar_id=\${u.calendar_id.padEnd(35)} guardado=\${(u.calendar_acceso||'none').padEnd(5)} detectado=\${r.padEnd(5)} (\${dt}ms)\`);
#       } catch (err) {
#         console.log(\`\${nombre} ERROR: \${err.message}\`);
#       }
#     }
#   })();
#   " 2>&1
#   EXIT=$?
#   echo ""
#   echo "node exit: $EXIT"

# ───── output ─────
═══ Estado DB ═══
id  nombre            calendar_id                     calendar_acceso
--  ----------------  ------------------------------  ---------------
1   Diego             diego@paez.is                   write          
2   Hernan Fulco      hernan.fulco@sondeosglobal.com  write          
5   Santiago Bignone  santiagocbignone@gmail.com      none           

═══ Chequeo real contra Google (timeout 60s) ═══
Diego                calendar_id=diego@paez.is                       guardado=write detectado=write (510ms)
Hernan Fulco         calendar_id=hernan.fulco@sondeosglobal.com      guardado=write detectado=write (287ms)
Santiago Bignone     calendar_id=santiagocbignone@gmail.com          guardado=none  detectado=write (292ms)

node exit: 0

# exit=0
