# ejecutado: 2026-08-16T19:06:02-03:00
# host: ubuntu-4gb-nbg1-2
# instancia: maria-paez
# script:
#   #!/bin/bash
#   cd /root/secretaria
#   [ -f state/.canary-bad-commit ] && { echo "CANARY BLOQUEÓ: $(cat state/.canary-bad-commit)"; exit 0; }
#   pm2 reload ecosystem.config.js --only maria-paez --update-env >/dev/null 2>&1 && echo "reload OK"
#   # test de las queries (sin LLM): pendiente temporal de un usuario fantasma esperando el numero de Diego
#   node -e "
#   const db=require('/root/secretaria/node_modules/better-sqlite3')(process.env.MARIA_DB);
#   db.exec('BEGIN');
#   try {
#     const digs='541132317896';
#     const vs=[digs,'549'+digs.slice(2)];
#     db.prepare(\"INSERT INTO pendientes (usuario_id, \\\"desc\\\", estado, meta_json) VALUES (99, 'TEST gestión ajena esperando a Diego', 'abierto', ?)\").run(JSON.stringify({esperando_de: digs+'@c.us'}));
#     const likeP = vs.map(()=>\"meta_json LIKE '%' || ? || '%'\").join(' OR ');
#     const r = db.prepare('SELECT id, usuario_id, \"desc\" AS descTxt FROM pendientes WHERE estado=\\'abierto\\' AND usuario_id != ? AND meta_json IS NOT NULL AND ('+likeP+') ORDER BY id DESC LIMIT 4').all(1, ...vs);
#     console.log('query pendientes:', r.length===1 && r[0].usuario_id===99 ? 'MATCH OK' : 'FALLO '+JSON.stringify(r));
#     const likeF = vs.map(()=>\"esperando_de LIKE '%' || ? || '%'\").join(' OR ');
#     const f = db.prepare('SELECT id FROM follow_ups WHERE estado IN (\\'abierto\\',\\'disparado\\') AND usuario_id != ? AND ('+likeF+')').all(1,...vs);
#     console.log('query follow_ups: OK (', f.length, 'reales )');
#   } finally { db.exec('ROLLBACK'); db.close(); }
#   "
#   echo LISTO

# ───── output ─────
reload OK
[eval]:7
  db.prepare("INSERT INTO pendientes (usuario_id, \"desc\", estado, meta_json) VALUES (99, 'TEST gestión ajena esperando a Diego', 'abierto', ?)").run(JSON.stringify({esperando_de: digs+'@c.us'}));
                                                                                                                                                   ^

SqliteError: FOREIGN KEY constraint failed
    at [eval]:7:148
    at runScriptInThisContext (node:internal/vm:209:10)
    at node:internal/process/execution:446:12
    at [eval]-wrapper:6:24
    at runScriptInContext (node:internal/process/execution:444:60)
    at evalFunction (node:internal/process/execution:279:30)
    at evalTypeScript (node:internal/process/execution:291:3)
    at node:internal/main/eval_string:74:3 {
  code: 'SQLITE_CONSTRAINT_FOREIGNKEY'
}

Node.js v22.23.1
LISTO

# exit=0
