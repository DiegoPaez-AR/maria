# ejecutado: 2026-08-02T19:48:02-03:00
# host: ubuntu-4gb-nbg1-2
# instancia: maria-paez
# script:
#   #!/bin/bash
#   set -e
#   NGCONF=$(readlink -f /etc/nginx/sites-enabled/intensa.io.conf)
#   echo "conf real: $NGCONF"
#   if ! grep -q "wa-hook" "$NGCONF"; then
#   python3 - "$NGCONF" <<'PY'
#   import sys
#   p = sys.argv[1]
#   s = open(p).read()
#   loc = """
#       # Webhook AutoResponder de Maria (2026-08-02)
#       location /hooks/wa-maria/ {
#           proxy_pass http://127.0.0.1:4501/wa-hook/;
#           proxy_read_timeout 90s;
#           proxy_http_version 1.1;
#       }
#   """
#   blocks = s.split('server {')
#   done = False
#   for i, b in enumerate(blocks):
#       if 'intensa.io' in b and ('443' in b or 'ssl' in b) and not done:
#           idx = b.rfind('}')
#           blocks[i] = b[:idx] + loc + b[idx:]
#           done = True
#   open(p, 'w').write('server {'.join(blocks))
#   print('location insertada' if done else 'NO ENCONTRE server block ssl')
#   PY
#   nginx -t 2>&1 | tail -1 && systemctl reload nginx && echo "nginx reloaded"
#   else
#     echo "ya estaba"
#   fi
#   SEC=$(grep WA_HOOK_SECRET /root/secretaria/config/secrets.conf | cut -d= -f2)
#   sleep 2
#   curl -s -m 15 -X POST "https://intensa.io/hooks/wa-maria/$SEC" -H 'Content-Type: application/json' -d '{"query":{"sender":"+1 555 000","message":"test","isGroup":false,"isTestMessage":true},"appPackageName":"x","messengerPackageName":"x"}'
#   echo

# ───── output ─────
conf real: /etc/nginx/sites-available/intensa.io.conf
location insertada
nginx: configuration file /etc/nginx/nginx.conf test is successful
nginx reloaded
{"replies":[{"message":"✅ Webhook de Maria conectado. Todo listo."}]}

# exit=0
