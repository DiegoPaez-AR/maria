# ejecutado: 2026-05-08T15:42:01-03:00
# host: ubuntu-4gb-nbg1-2
# script:
#   #!/bin/bash
#   set +e
#   DB=/root/secretaria/db/maria.sqlite
#   
#   echo "── usuarios y su calendar_acceso ──"
#   sqlite3 -header -column "$DB" "
#   SELECT id, nombre, rol,
#          COALESCE(email,'(sin email)') AS email,
#          COALESCE(calendar_id,'(sin calendar_id)') AS calendar_id,
#          calendar_acceso,
#          activo
#   FROM usuarios
#   ORDER BY id;
#   "
#   echo
#   echo "── últimos cambios detectados por calendar-watch (eventos sistema) ──"
#   sqlite3 -header -column "$DB" "
#   SELECT timestamp, usuario_id, substr(cuerpo,1,80) AS cuerpo
#   FROM eventos
#   WHERE canal='sistema'
#     AND cuerpo LIKE '%calendar_acceso%'
#   ORDER BY id DESC
#   LIMIT 20;
#   "
#   echo
#   echo "── log fresh con líneas calendar-watch ──"
#   pm2 logs maria --lines 1500 --nostream 2>&1 | grep -E 'calendar-watch' | tail -20

# ───── output ─────
── usuarios y su calendar_acceso ──
id  nombre          rol      email                           calendar_id                     calendar_acceso  activo
--  --------------  -------  ------------------------------  ------------------------------  ---------------  ------
1   Diego           owner    diego@paez.is                   diego@paez.is                   write            1     
2   Hernan Fulco    usuario  hernan.fulco@sondeosglobal.com  hernan.fulco@sondeosglobal.com  write            1     
3   Natali Funez    usuario  nfunez@iplan.com.ar             (sin calendar_id)               none             1     
4   Fernando Boero  usuario  (sin email)                     (sin calendar_id)               none             1     

── últimos cambios detectados por calendar-watch (eventos sistema) ──

── log fresh con líneas calendar-watch ──
0|maria    | 2026-05-08 15:01:09: ▸ arrancando calendar-watch (cada 8h)
0|maria    | 2026-05-08 15:01:09: [calendar-watch] activo, cada 8.0h

# exit=0
